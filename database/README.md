# Database

Sakuta currently uses Supabase. The authoritative database migrations remain at `/supabase/migrations` so Supabase CLI tooling keeps its expected convention.

Do not duplicate migration files in this directory.
